# 주문과 로그인 기준 (구현 전)

## 로컬 장바구니와 카드

통합 장바구니는 [DATABASE](DATABASE.md)의 로컬 작업목록이다. AUTO 수량 `+/-`는 로컬 DB만 수정한다. 이 동작에서 웹사이트 접속, Playwright 실행, 네트워크 요청을 금지한다.

전체 주문은 [UI](UI.md)의 진행창에서 순차 처리한다. AUTO는 공급처 하나의 주문 가능 상품을 카드 하나로, MANUAL_BROWSER는 상품 URL 하나를 카드 하나로 처리한다. 병렬 결제와 사용자 순서 변경은 없다. 각 카드의 개별 주문도 지원한다.

성공이 확실한 카드만 완료 처리하여 진행창에서 제거하고, 해당 주문만 원래 장바구니에서 제거한다. FAILED/UNKNOWN은 남긴다. MANUAL 완료 확인은 [COUPANG_NAVER](suppliers/COUPANG_NAVER.md)를 따른다.

## AUTO 실행 순서

1. 최신 상품 상태 확인.
2. 주문 대상 확정.
3. 로컬 SQLite 주문 스냅샷/체크포인트 저장 성공 확인.
4. 실제 사이트 장바구니 정리.
5. CafeOrder 주문대상만 다시 담기.
6. 실제 사이트 장바구니 검증.
7. 가능한 가장 빠른 실제 배송일 선택.
8. 체크아웃.
9. 필요한 경우 사용자 결제/인증 대기.
10. 실제 주문 성공 여부 확인.
11. 성공 시 주문기록 저장 및 해당 장바구니 항목 제거.

3번 로컬 저장 실패 시 원격 장바구니를 건드리지 않는다. 최종 장바구니 검증은 기본적으로 상품 식별과 수량만 비교하며 옵션 자체는 비교조건으로 만들지 않는다. 검증 실패 시 자동 재구축은 최대 한 번이다. 두 번째 검증도 실패하면 해당 공급처만 실패시키고 다른 공급처는 계속할 수 있어야 한다.

## 결제 안전과 상태

사용자 표시 상태: `PENDING`, `PREPARING`, `WAITING_FOR_USER`, `PAYMENT_IN_PROGRESS`, `COMPLETED`, `FAILED`, `UNKNOWN`.

결제 제출 가능성이 있으면 무조건 재결제하지 않는다. 성공 페이지를 놓쳤거나 결과가 불확실하면 공급처 주문기록부터 확인한다. 주문 존재가 확실하면 COMPLETED, 주문 부재가 확실하고 재시도가 안전할 때만 재시도 가능, 판단 불가이면 UNKNOWN으로 남긴다.

## AUTO 로그인 유지

공급처별 Playwright persistent browser profile을 사용할 수 있도록 설계한다. 쿠키/세션/localStorage 등 로그인 상태는 사용자 PC에 로컬 저장할 수 있다. 자격정보/백업/로그 보안은 DATABASE를 따른다.

앱 시작 시 화면과 로컬 상품/장바구니를 먼저 보여주고 AUTO 로그인 확인은 공급처별 백그라운드에서 진행한다. 무거운 브라우저 작업의 동시 실행은 기본 1~2개 수준을 고려한다. OTP, QR, CAPTCHA, 추가 본인인증은 우회하지 않으며 브라우저를 사용자에게 보여주고 WAITING_FOR_USER로 전환한다.

## 무료배송 설정

사이트에서 기준을 자동 수집하지 않고 CafeOrder 설정값을 사용한다. 초기값은 각 공급처 문서에 둔다. 사용자는 사이트관리에서 수정할 수 있어야 한다. 쿠팡/네이버에는 자동 무료배송 조건을 적용하지 않는다.
